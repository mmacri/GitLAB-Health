pages:
  stage: deploy
  image: alpine:3.20
  script:
    - mkdir -p public
    - cp -R assets data print public/
    - cp index.html cheatsheet.html .nojekyll public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
