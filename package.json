{
  "name": "gitlab-health",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test src/tests/*.mjs",
    "check": "node --test src/tests/*.mjs",
    "integrity": "node scripts/integrity-check.mjs"
  }
}
